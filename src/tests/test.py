import unittest
from main import add

class TestMathFunctions(unittest.TestCase):
        def test_add(self):
                pass
                # result = add(1,5)
                
                # self.assertEqual(result, 6, msg="Add function did not return 6 when given arguments 1 and 5")

                # result = add(-1,1)
                
                # self.assertEqual(result, 0, msg="Add function did not return 0 when given arguments -1 and 1")

                # result = add(0,0)
                
                # self.assertEqual(result, 0, msg="Add function did not return 0 when given arguments 0 and 0")

